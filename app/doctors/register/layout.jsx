export const metadata = {
    title: "Register Doctor",
    description: "Register a new Doctor"
};

export default function RegisterDoctorLayout({ children }) {
    return (
        <main >
            {children}
        </main>
    );
}